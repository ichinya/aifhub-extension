<!-- chunk: c017 -->
# Acme Orders

This synthetic service receives order commands, validates sessions and records audit events.

<!-- chunk: c018 -->
## Operator commands

Use `npm test` for unit tests and `npm run validate` for repository contracts. Runtime state is not product documentation.
